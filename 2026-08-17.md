In order to have a transformation to prepare the atomic_importer and path_reffs for insertion to the prefix I need to define the list of publicly accessable calls. then a loop can add @internalmethod or @externalmethod accordingly to each ast recognized function vs lines numbers in range according to whether that function name is on the public facing list or not.

Each module file can get the import block removed via a convenient # === Start Here === line.

for nested classes the @internalmethod or @externalmethod will govern whether those classes go to the public or internal class. But do not apply static method. stuff within a nested class must not be modified by the transpiler.
