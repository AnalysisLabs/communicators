to make progress forward we need to define appropriate (temporary) intermediate representations to use to get to our desired  code gen. 
- for one thing save intermediates to disk enough though final version will stay in ram.
- This includes not just intermediate frameworks but even any addition or change to generated code like adding a new block of code not even running them. this is sub optimal but it scales better.
- pass high_transpiler which really is done as high level with an os exec passes to a lower or medium level transpiler which actually juggles the code blocks.
- in keeping with above the IR here should also be dumped to disk as ir.