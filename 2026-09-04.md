Here are our principles. Each connection type has a very finite list of required parts of the sequence to exist. there is much overlap amoun types but thsi is not perfect. this must be made into a table. The L1 transponder_slots.py must define these methods for each slot type as (tentatively a class) functions.

Alternatively these slots will be merely defined comment banners.

At any rate once these are defined we should have more straight forward L2.

The method is to have these formalized one by one and then syncronize them into a tabel where I sense overlap.

Only once I have these universal necessities can I robustly abstract to make L2. 

This implies a way to prototype test the implied tabel of options, but how?

fundamentally L3/core only contributes value when there is multiplex routing and or backlog. Thus there is no implediment in principle to testing each level in isolation. Or at least testing the transponder_wire.py without core.py as an incremental test. To the degree the slots depend on the wiring for the spine of the communicatorss even without multiplex routing and backlog handling.

So big deal, transponder_master.py is not incrementally necessary except as a flare that will only bear value under multiplexing and high frequency messeges! We could push this further to make L2 optional but that would evolve non-extensible spines in the program that uses them, though the learning curve might cost effectively transfere.

aside: **I am getting sleepy, but the work still has to be done by me. I think.**

Indeed, what else still has to be done by me?

Perhaps I ought to make a skeleton for this code.

Indeed, I ought to make a sequence of operations in L2, but the individual operators in tables.

What, then, remains of L1 as master? I think it would be a place-holder and simplicity for future thoughts.

But by way of operation, receptacle and announcement exist as written for certainty in a declarative mode, not an operative one.

Therefore the question arises: what is the connection between these? What is the order of causality? They inhabit the same place / the same identity. It is not beautiful, but it operates sufficiently for the ends. In what way ought I to join these operations? By calling between functions.
