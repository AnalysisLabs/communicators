Macro-goal. Get egg_transpiler import namespace.py and send the generated caterpilar_transpiler.py via that to be saved to the metamorphosis.db

One method to address this is to quiz grok on the gap between status quo and this macro-goal.

Another is to examine the code myself. 

After myown examination we look ready to to run a test so I wil quiz Grok, but only as a double check.

Grok is confused, most of that should be fixed by a run. The main problem is unearthed is that build_real_nodes.py does not exist.

Now we have a Metamorphosis DB write failed (NameError: name 'prefix' is not defined); falling back error. ✓

Next ValueError: Invalid lines in /home/prometheusd/Analysis Labs/Dev Tools/com-branches/staged/staged-1/communicators/Metamorphosis/transpiler-methods/panel.md:
Line 8: