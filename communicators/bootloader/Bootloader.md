First run.sh insures the import dependencies are installed.

then the bootloader runs.

The bootloader:

initializes the communicator-ram dir in dev/shm.

initializes finder.json in dev/shm/communicator-ram

(possible) initializes finder.py in dev/shm/communicator-ram

loads runtime namespace.py to dev/shm/communicator-ram

runs runtime namespace.py

def: from now on run implies loading runtime modules to dev/shm/communicator-ram and then running that.

run egg

run caterpillar

run chrysalis

run butterfly

run homeostasis
