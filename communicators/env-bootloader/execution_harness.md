1. Merge in order the code from /home/guatamap/Analysis Labs/Dev Tools/com-branches/orchestrated-1/communicators/env-bootloader/prefix.py and the path passes as the program argument. 
2. then save the result to /home/guatamap/Analysis Labs/Dev Tools/com-branches/orchestrated-1/communicators/temp and execute. 
3. Don't run the copy, run the original code generated in RAM. 