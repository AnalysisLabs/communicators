# Metamorphosis/transpiler — Place in the grand scheme

Program and topology transformation entrypoints (egg/caterpillar-style
pipelines). They consume declarations or graphs and emit forms the rest of the
system can run or reconcile.
