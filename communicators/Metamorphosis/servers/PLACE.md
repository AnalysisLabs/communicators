# Metamorphosis/servers — Place in the grand scheme

Server entrypoints and service modules that run as distinct processes or
long-lived roles after ignition. Launched via Genesis execution facilities;
defined here because they are product/orchestration behavior, not substrate.
