# 2026-09-15 — Transponder prefix, this session

Handoff for the next grok session. The human will likely revert
`staged-2-grok` to pre-session state. Do not treat the hatch rewrites
in this worktree as proven.

What Transponder *is* (layers, polarity, prefix join, proven vs in
flight): `communicators/Genesis/internal_imports/edge-methods/Transponder.md`

## Job (still open)

Make the Genesis prefix **with transponder modules** valid Python so
Communicators OS can exec it. Success: from `communicators/`, `./run.sh`
gets past Stage B hatch construct (`_Name_internal = Name_internal()`)
without TypeError / SyntaxError / NameError from the transponder stack.

Optional later, not required: egg_transpiler sending
`caterpillar_transpiler.py` over one of the four live media into
`metamorphosis.db`.

## Constraints the human already decided

- Thin `@externalmethod` pass-throughs onto `_Name_internal`. Zero-arg
  internal `__init__`. Do not fork Stage B. Do not park `__init__` on
  the public class.
- Live prefix media: **tcp, unix, websocket, shm**. HTTP / mailbox /
  station-tuner stay on disk under `transponder_stack/` but **off
  `MEMBERS`**.
- `SlotRefused` as an Exception is **not** gospel (a prior grok invented
  that). No hard human opinion; Stage B drops bases anyway.
- `prefix_builder.py` is editable in this grok sandbox (human said so
  after a rejected first cut). Other grok-sandbox files are fair game
  unless the human names them off-limits.
- Philosophy: Prefix_Tier, Internal_Import, Runtime_Context apply.
  Do not skip those three for this job.

## What blocked this session

Sandbox profile `comms` (`extends = "strict"`) did not include Nix.
Landlock denied `/nix/store/.../bash`. Even `echo hello` failed.
Human updated `.grok/sandbox.toml` (add `/nix`, `/nix/store`,
`/run/current-system` read_only; `/nix/var` and `~/.cache/nix`
read_write). **This session cannot pick that up.** Start a **new**
session. If `./run.sh` still cannot run, complain immediately
(AGENTS.md). Next clue: `~/.grok/sandbox-events.jsonl`.

## What the human will likely do before you start

Revert this worktree. Re-apply the algorithm with a working shell:
edit, `./run.sh`, read the traceback, repeat. Do not commit until
that loop is green.

## Run

```bash
cd "/home/prometheusd/Analysis Labs/Dev Tools/com-branches/staged/staged-2-grok/communicators"
./run.sh
```
