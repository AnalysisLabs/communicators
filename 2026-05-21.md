lesson: my confusion was due to having multiple mutually exclusive ways of moving forward, without clearly understanding this. hence the problem was locally undecidable and therefore exhausting.

the proto-namespace(object) vs namespace distinction is not clear:

-   one could draw the line at disk vs ram x
-   one could draw the line at the naming convention of namespace has to have namespace in its title x
-   I could merge the concepts of object_program and from_namespace.
    -   Well is there any valid distinction between them
    -   Better yet is there any possible circumstance were I would need to draw a distinction?

            if NO

            ∴ Merge

            else

            ∴ Define the difference
There could be reason for difference under assumption that i want to maintain sharp RAM vs disk distinction though. File ext kind of destroys that argument. Contra I would need to decide on intra namespace distinction. ∴ keep object as unique namespace ⊆ from_namespace.

object program like mermaid | with program like generate topology (which transforms the proto-namespace) |

solution: path-like namespace under BaseNamespace. Thus namespace is not mutually exclusive but allows ⊆ with is members.