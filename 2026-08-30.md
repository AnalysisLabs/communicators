## Stream of consciousness

In order to decide how to refactor the load and build functions I must first evaluate from first principles what arguments are critical to specification in general and what are redundant and compressable. load is to move a program from disk to DB. Build it to  take a program from DB and use it as spec to build a new program. Thus load only required the references to the location of the disk file and where it needs to go in DB or the converse. Build requires the spec program, transformer program, and output program as references

therefore all namespace.py need to do is to be able to accept a  code blob or file_ref and the DB ref and a command to pull the contents, or save. But since pulling contents never requires a file_ref or code blob, the presence or absense of that wll be enough to rout to saving vs pulling a code blob from the DB.