Now I have the simple high level transponder module. I still need to define create_listener(ip, port), accept_connection(listener), connect_to(host, port), sendall(data), recv(size)

So good news, I have made progress. 

Next up define create_listener(ip, port)